## Forex like trading simulator
#### Will utilize some API to fetch currency rates and will store them in the database for later. The idea is to simulate trade operation such as 'buying', 'selling' and etc.

```
npm run install
npm run start
```
